# Dependências da Aula 02 — Modelos de Processamento (SIMD, MIMD, RISC, CISC).
# Instaladas automaticamente pelo iniciar.bat (dentro de .venv).
# NumPy é obrigatório; CuPy/PyTorch são opcionais (usados só se houver GPU).
numpy>=1.24.0
