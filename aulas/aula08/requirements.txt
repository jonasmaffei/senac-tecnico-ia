# Dependências da Aula 08 — Manipulação de Memória em CUDA (Tiling).
# Instaladas automaticamente pelo iniciar.bat (dentro de .venv).
# numpy é obrigatório; numba e cupy são opcionais (só usados com GPU NVIDIA).
numpy>=1.24.0
