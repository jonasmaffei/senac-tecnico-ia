# Dependências do laboratório Windows — Aula 09.
# Instaladas automaticamente pelo iniciar.bat (dentro de .venv).
# pyopencl usa os drivers OpenCL já presentes no Windows (AMD/Intel/NVIDIA).
# Sem pyopencl, os scripts mostram o conceito e a referência.
numpy>=1.24.0
pyopencl>=2023.1
