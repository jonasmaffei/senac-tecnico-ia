# ============================================================
# Dependências — Introdução a Arquitetura de Computadores (Senac)
# Instale com: pip install -r requirements.txt
# ============================================================

# --- Obrigatórias (funcionam em qualquer máquina) ---
numpy>=1.24.0

# --- Requerem GPU NVIDIA com drivers CUDA instalados ---
# PyTorch: instale pela página oficial https://pytorch.org/get-started
# O comando varia conforme sua versão de CUDA. Exemplo para CUDA 12.4:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0

# Numba: compilador JIT com suporte a kernels CUDA
numba>=0.57.0

# CuPy: NumPy acelerado por GPU (escolha a versão compatível com seu CUDA)
# Exemplo para CUDA 12.x:
#   pip install cupy-cuda12x
# cupy-cuda12x>=12.0.0
